<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Start Page" Width="800" Height="600">
    <Grid Background="WhiteSmoke">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Background="WhiteSmoke">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Welcome to RecipeOrganizer" FontSize="20" Margin="0 0 0 20" HorizontalAlignment="Center"/>
            <Button Grid.Row="1" Content="Log In" Click="LogInButton_Click" Width="100" Height="40" Margin="0 0 0 20" Background="DarkBlue" Foreground="White"/>
            <Button Grid.Row="2" Content="Sign Up" Click="SignUpButton_Click" Width="100" Height="40" Background="DarkBlue" Foreground="White"/>
        </Grid>
    </Grid>
</Window>